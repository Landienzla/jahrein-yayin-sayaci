import React from 'react'
import{ref,set} from 'firebase/database'
import {database} from '../firebase'
export default function realtimedb() {
    // set(ref(database,'botSettings'),{
    //     tweetTime: "13:31"
    // })
    return (
        <div>
            
        </div>
    )
}
